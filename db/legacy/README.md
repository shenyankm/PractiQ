# Historical multi-user schema

Archived for reference only. These SQL files describe the removed Java/WeChat product and are NOT part of personal-database initialization or `make verify`. Never apply them to `practiq_personal` or use them to reset an existing database. The active, fresh-only schema is `../00_schema.sql`.
