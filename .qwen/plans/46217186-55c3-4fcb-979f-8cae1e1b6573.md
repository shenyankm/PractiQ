## 清理计划:移除本地开发无关的生产/运维/性能文件

范围:A 生产部署链 + B 运维观测配置 + D 性能压测工具链;`deployment-hardening.test.ts` 整个删除。

### 一、git rm 删除的文件/目录

**A 生产部署链**
- `openwook.service`
- `openwook@.service`
- `openwook-import-worker.service`
- `scripts/deploy/openwook-systemd.sh`

**B 运维观测配置**
- `ops/observability/`(整目录 9 文件:alerts.yml、docker-compose.yml、fluent-bit.conf、journald.conf.example、otel-collector.yml、postgres-observability.sql、postgresql.conf.example、prometheus.yml、README.md)
- `docs/observability.md`

**D 性能压测工具链**
- `scripts/perf/`(整目录 5 文件:observe-run.sh、prepare-data.mjs、quick-regression.mjs、run-suite.mjs、stress-runner.mjs)

**测试**
- `tests/deployment-hardening.test.ts`(整个删)

**空目录**:A+D 删后 `scripts/` 空 → 删 `scripts/`;B 删后 `ops/` 空 → 删 `ops/`。

### 二、修改的文件

1. **`package.json`** — 删 `perf:prepare`、`perf:suite`、`perf:quick` 三条 script(其余 db:*/worker:* 不经 scripts/,不受影响)。

2. **`tests/observability.test.ts`** — 删最后一个 `it` 块("documents runtime observability…",约 69–82 行)。保留前 4 块(均断言应用代码 logger/api/metrics/db/redis/health route)。**此举同时修复上一轮遗留的 C 类回归**(第 74 行 fluent-bit.conf 断言位于该块内)。

3. **`README.md`**
   - 删第 154 行 `Production deployments should run the worker as a separate process. A systemd template is provided at \`openwook-import-worker.service\`.`(保留前文本地 `pnpm dev` / `pnpm worker:imports` 说明)
   - "Observability and Operations" 节:保留应用层能力 6 条 bullet(pino/x-request-id/health/metrics/OTel/db-redis metrics);删 "Recommended production stack" 段及 5 条 + `Sample … live in ops/observability/` 句;`journald/stdout` → `stdout`
   - 删 "Performance Operations" 整节(systemd 多实例 + openwook-systemd.sh 命令 + perf 命令 + 连接池说明)

### 三、不动的文件(确认非冗余)
`instrumentation.ts`(OTel,启动注册、无 endpoint 时不导出)、`proxy.ts`(会话中间件)、`.env.example`、应用层 metrics/health 代码、`.gitignore` 的 `.omx/`。

### 四、验证
1. 全工作区 grep 残留:`openwook\.service|openwook-systemd|ops/observability|scripts/perf|"perf:|observability\.md`(应只剩 docs/system-design.md 等无关命中,逐条确认)
2. `pnpm test` — observability.test.ts 前 4 块应通过,deployment-hardening 已不存在
3. `pnpm lint`

> 注:`.gitignore` 的 `/reports/` 行(perf 产物目录)留着无害,本轮不动;若想彻底可一并清,你说一声。